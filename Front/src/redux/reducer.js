
const initialState = {

}

const reducer = (state = initialState, { type, payload }) => {
    switch (type) {

        // case example:
        //     return {

        //     }
            
        default:
        return { ...state }
    }
}

export default reducer